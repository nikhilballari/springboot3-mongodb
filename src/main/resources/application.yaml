server:
  port: 8090

spring:
  application:
    name: springboot3-mongodb
  data:
    mongodb:
      authentication-database: admin
      username: admin
      password: password
      database: demo
      port: 27017
      host: localhost
  servlet:
    multipart:
      max-file-size: 256MB
      max-request-size: 256MB
      enabled: true